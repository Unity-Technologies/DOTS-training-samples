using Unity.Mathematics;

public static class Utils
{
    //public static float3 AASignedDistanceToTarget(this Bounds box, float3 position, float3 target)
    //{
    //    return target - (position + box.Value.Center) - box.Value.Extents;
    //}
}
